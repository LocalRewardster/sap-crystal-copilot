# Windows-compatible requirements for Crystal Copilot
# This version avoids Rust compilation issues on Windows

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.4.2  # Older version with pre-compiled wheels
pydantic-settings==2.0.3

# File handling and parsing
python-multipart==0.0.6
aiofiles==23.2.1
xmltodict==0.13.0

# AI and LLM integration
openai==1.3.0
anthropic==0.7.8
httpx==0.25.2
requests==2.31.0

# Database and storage (Windows compatible versions)
sqlalchemy==2.0.23
alembic==1.13.1
# Skip asyncpg for Windows, use psycopg2 instead
psycopg2-binary==2.9.9
redis==5.0.1
supabase==2.3.4
postgrest==0.13.2

# Security and authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Monitoring and observability
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-instrumentation-fastapi==0.42b0
prometheus-client==0.19.0

# Utilities
python-dotenv==1.0.0
structlog==23.2.0
typer==0.9.0
rich==13.7.0

# Testing (optional for Windows)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0

# Development tools (optional for Windows)
black==23.11.0
isort==5.12.0
flake8==6.1.0
# Skip mypy for Windows compatibility