# Ultra-simple Windows requirements - no compilation needed
# This version uses only pure Python packages or pre-compiled wheels

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.4.2
pydantic-settings==2.0.3

# File handling and parsing
python-multipart==0.0.6
aiofiles==23.2.1
xmltodict==0.13.0

# AI and LLM integration
openai==1.3.0
anthropic==0.7.8
httpx==0.25.2
requests==2.31.0

# Database and storage (REST API only - no PostgreSQL drivers)
redis==5.0.1
supabase==2.3.4
# Note: We'll use Supabase REST API instead of direct PostgreSQL connection

# Security and authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Monitoring and observability
prometheus-client==0.19.0

# Utilities
python-dotenv==1.0.0
structlog==23.2.0
typer==0.9.0
rich==13.7.0

# Testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0

# Development tools (optional)
black==23.11.0
isort==5.12.0
flake8==6.1.0