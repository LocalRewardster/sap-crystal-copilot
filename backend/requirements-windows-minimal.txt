# Minimal Windows requirements - Python 3.13 compatible
# Uses only packages with guaranteed pre-compiled wheels

# Core web framework - older versions with better wheel support
fastapi==0.100.1
uvicorn==0.23.2
pydantic==1.10.12  # v1 has better Windows wheel support
pydantic-settings==1.10.12

# File handling
python-multipart==0.0.6
aiofiles==23.2.1

# HTTP clients
httpx==0.24.1
requests==2.31.0

# Supabase (pure Python client)
supabase==1.3.0

# Utilities (pure Python)
python-dotenv==1.0.0
rich==13.7.0

# Basic crypto (should have wheels)
passlib==1.7.4

# Development
pytest==7.4.3